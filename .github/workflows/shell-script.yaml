name: shell-script
on:
   workflow_dispatch:
jobs:
  job1:
    name: using shell-script
    runs-on: ubuntu-latest
    steps:
      - name: actions
        uses: actions/checkout@v4
      - name: check the file
        run: ls -la
      - name: run cmds
        run: |
               chmod +x shell.sh
               ./shell.sh

     
